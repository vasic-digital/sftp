# mobile/ — Mobile clients

Kotlin Multiplatform mobile app (Android/iOS) consuming the management API; shared KMP modules are incorporated here by this stream.

Owned by STREAM-4 (mobile) per `docs/plans/master_implementation_plan.md`.
