# api/ — SFTP Enterprise Management API (Go)

Go REST API managing SFTP users, sessions, audit, and system config. Depends on the owned `digital.vasic.*` submodules at the repo root (see `go.mod` replace directives).

Owned by STREAM-2 (API core) per `docs/plans/master_implementation_plan.md`; module skeleton + dependency graph bootstrapped by STREAM-1 (ATM-001).
