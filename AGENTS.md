> **Base agent rules:** `constitution/AGENTS.md` — READ IT FIRST.
> The base file is authoritative for any topic not covered here.
> Project-specific rules below extend them; they never weaken them.

---

## SFTP Service — Agent Instructions

This is a containerized SFTP server deployment project. Tasks involve:
- Editing `docker-compose.yml`, `.env`, `users.conf`, `sshd_config`
- Managing `data/` user directories and permissions
- Updating deployment documentation
- All work happens in the parent project root (not inside the constitution submodule)
