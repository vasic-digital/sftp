# web/ — Web admin console

Browser-based SFTP management UI (React + OpenDesign tokens from `open_design/` submodule).

Owned by STREAM-3 (web console) per `docs/plans/master_implementation_plan.md`.
