# qa/ — QA evidence and HelixQA/Challenges run artifacts

Per-run end-user evidence (`docs/qa/<run-id>/` transcripts per §11.4.83) plus HelixQA/Challenges execution outputs curated at release prep. Raw captures stay git-ignored (`qa-results/`).

Owned by STREAM-5 (QA automation) per `docs/plans/master_implementation_plan.md`.
